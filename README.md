# API Docs Lie Detector

Exposes when API documentation doesn't match reality. Because trusting docs is like trusting a weather forecast from last year. Run: `python api_lie_detector.py <url> '<expected_json>'`